import { IRequest } from "../../request";
import { IQueryBuilder } from "../interface";
export declare class GenericQueryBuilder<T extends IRequest> implements IQueryBuilder<T> {
    build(parameter: T extends IRequest ? any : any): string;
}
