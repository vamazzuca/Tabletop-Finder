import { JsonParser } from "jackson-js";
import { BggHotDto } from "../../concrete";
export class BggHotDtoParser {
    constructor() {
        this.parser = new JsonParser();
    }
    jsonToDto(jsonData) {
        return new Promise((resolve) => {
            resolve(this.parser.transform(jsonData.items[0].item, {
                mainCreator: () => [Array, [BggHotDto]]
            }));
        });
    }
}
//# sourceMappingURL=BggHotDtoParser.js.map