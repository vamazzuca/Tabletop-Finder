import { JsonParser } from "jackson-js";
import { BggThingDto } from "../../concrete";
export class BggThingDtoParser {
    constructor() {
        this.parser = new JsonParser();
    }
    jsonToDto(jsonData) {
        return new Promise((resolve) => {
            resolve(this.parser.transform(jsonData.items[0].item, {
                mainCreator: () => [Array, [BggThingDto]]
            }));
        });
    }
}
//# sourceMappingURL=BggThingDtoParser.js.map