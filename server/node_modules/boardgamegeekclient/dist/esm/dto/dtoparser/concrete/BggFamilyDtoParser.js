import { JsonParser } from "jackson-js";
import { BggFamilyDto } from "../../concrete";
export class BggFamilyDtoParser {
    constructor() {
        this.parser = new JsonParser();
    }
    jsonToDto(jsonData) {
        return new Promise((resolve) => {
            resolve(this.parser.transform(jsonData.items[0].item, {
                mainCreator: () => [Array, [BggFamilyDto]]
            }));
        });
    }
}
//# sourceMappingURL=BggFamilyDtoParser.js.map